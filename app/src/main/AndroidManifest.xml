<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- 1. 물어보기 관련 퍼미션 : STT, TTS, ChatGPT-->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 2. 관련 퍼미션 : -->

    <!-- 3. 원격 관련 퍼미션 : -->
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mumulbo2023"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.mumulbo2023.AddPersonActivity"></activity>
        <activity android:name="com.example.mumulbo2023.SettingActivity"></activity>
        <activity android:name="com.example.mumulbo2023.PleaseActivity"></activity>
        <activity android:name="com.example.mumulbo2023.ActMmbActivity"></activity>
        <activity android:name="com.example.mumulbo2023.AskMmbActivity"></activity>

    </application>

</manifest>